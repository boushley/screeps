{
  "name": "boushley-screeps",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Screeps game scripts in TypeScript",
  "scripts": {
    "build": "rollup -c",
    "copy": "npm run build && cat dist/main.js | pbcopy && echo 'Script copied!'",
    "watch": "rollup -c --watch"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.0",
    "@types/screeps": "^3.3.0",
    "rollup": "^4.30.0",
    "tslib": "^2.8.0",
    "typescript": "^5.7.0"
  }
}
