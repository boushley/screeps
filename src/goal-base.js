'use strict';
(function() {
    class GoalBase {
        constructor(memory, spawn) {
            this.memory = memory;
            this.spawn = spawn;
        }
    }

    module.exports = GoalBase;
})();
